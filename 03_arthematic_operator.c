#include <stdio.h>

int main(){
    int a=6;
    int b=4;
    int c= a+b;
    printf("the value of a is %d and the value of b is %d and the sum of the valuue is %d\n", a , b, c);
    // modulus operater is to get the remainder
    printf("the remainder when a is divide by b is %d \n",a%b);
    return 0;
}
    // if we use alt shift + down arrow the we put the same ine in down 
    // and we us alt when the coursour is blink then we put the same blink point on the other thing when we
    // write same thing on other for easily work
    // this dose not work as exepontation function
    // int d=a^b;