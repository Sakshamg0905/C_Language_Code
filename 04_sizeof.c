#include<stdio.h>
int main()
{
    printf("size of int: %zu bytes\n", size of (int));
    printf("size of float: %zu bytes\n", size of (float));
    printf("size of char: %zu bytes\n", size of (char));
    return 0;
}