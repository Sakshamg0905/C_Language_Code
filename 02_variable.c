#include<stdio.h>
int main()
the rules we are disscus about int
{
    we write
    int a;
    int b;
    int c;
    int saksham;
    int saksham_good;

    and not thid things like

    int saksham gis good;
    int saksham (*&*);
    int saksham ((())):
    like that thins we are disscuss about the thats rules is in
}