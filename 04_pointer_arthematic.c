#include <stdio.h>

int main(){
    //it is for an arthematic integer
   /*nt a=4;
    int* b=&a;
    printf("the address of a is %u\n",&a);
    printf("the address of b is %u\n",b);
    b++;
    printf("the address of b is %u\n",b);*/

    //it is for an arthematic char
    char a=4;
    char* b=&a;
    printf("the address of a is %u\n",&a);
    printf("the address of b is %u\n",b);
    b++;
    printf("the address of b is %u\n",b);
    return 0;
}