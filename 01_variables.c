#include <stdio.h>

int main(){
    int i=10; // declare and initilize 'i' with 10
    int j=i;
    // %d is for formate specifire
    // %d for in, %f for float and %c for char
    printf("the value of i is %d and j is %d",i,j);
    return 0;
}